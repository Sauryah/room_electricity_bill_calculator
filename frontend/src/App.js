// The actual Bill Splitter app lives entirely in /public/index.html.
// This React shell intentionally renders nothing so the static HTML drives the UI.
function App() {
  return null;
}

export default App;
